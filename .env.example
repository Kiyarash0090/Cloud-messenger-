# Environment configuration for Cloud Messenger

# TRUST_PROXY: Set to "1" ONLY when a reverse proxy you control sets X-Forwarded-For.
# When unset or "0", client-supplied X-Forwarded-For headers are ignored.
TRUST_PROXY=0

# NODE_ENV: Set to "production" in production environments.
NODE_ENV=development
